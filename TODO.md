# TODO

Open work on this artifact after the September 2026 revision, ordered by how
much it affects the paper's claims. Setup facts are documented in
[`docs/experimental_setup.md`](docs/experimental_setup.md).

## A. Document context: the fuller extension

The two-arm ablation on the PURE cell is done and reported
([`docs/context_ablation.md`](docs/context_ablation.md)). Still open:

1. A **marker-flipped** third arm (same context, mandatory and optional
   markers swapped) to isolate the author's marker from heading and
   neighbours.
2. The remaining envelope factors as a fractional design with pre-registered
   cells:

   | Context factor | Levels (sketch) |
   | --- | --- |
   | Document status | none / `Draft` / `Approved` |
   | Stakeholder role | none / product owner / end user / support engineer |
   | Priority field | none / `High` / `Low` |
   | Rationale sentence | absent / present |
   | Neighbouring requirements | none / two same-strength / two mixed-strength |
   | Elicitation transcript | absent / short interview excerpt containing the wish |

3. A second track on **naturally occurring stakeholder statements** (issue
   trackers, interview transcripts, feature requests) with annotated intended
   strength, reported separately from the controlled benchmark. Candidates are
   surveyed in
   [`docs/internal/external_validity_datasets.md`](docs/internal/external_validity_datasets.md).

## B. Human validation before submission

The author confirmed the human construct validation on 2026-09-04 and intends
to repeat it before submission, for the weak-intent templates
(`docs/weak_modality_construct_review.csv`) and for the 51 revised PURE
capabilities (`docs/pure_capability_revisions.csv`). The AI-assisted reviews
stay labelled as such ([`docs/validation_review.md`](docs/validation_review.md));
no independent two-human agreement is claimed.

## C. Archive links into the manuscript

Once the Zenodo records exist, the manuscript's data-availability statement
should name the code and dataset DOIs; it currently points at GitHub only.

## D. Small loose ends

- `outputs/weak_modality_probe/phrasing_probe_nine_models.csv` (local only)
  is a hand-assembled concatenation of the nine per-run probe summaries with
  no writer in the repository. Either add the writer to
  `scripts/run_weak_modality_probe.py` or delete the file.
- Local run registries still hold `complete` rows dated 2026-05-21 whose raw
  rows were removed later; the exporter refuses them, but they should be
  pruned.
- `scripts/populate_notebooks.py` lets nbformat assign fresh random cell ids
  on every regeneration, so every notebook shows a diff even when only one
  cell changed. Deterministic ids would keep those diffs minimal.

## Done in the September 2026 revision

- Primary protocol changed to one item per request with one deterministic and
  five sampled answers; request seeds sent and served model recorded, which
  closes the provenance gap of the May campaign.
- Cohort widened from six models of two families to nine models of five.
- Request-composition ablation run on eight models (sizes 4 and 16, grouped
  and sibling-separated, against a single-item reference); document-context
  ablation on eight models; phrasing probe on all nine.
- Every paper table regenerated by `scripts/export_paper_tables.py` and
  tracked with its provenance; the README quotes the tracked values under
  test.

## Out of scope: fine-tuning

The study measures off-the-shelf behaviour of instruction-tuned models under
a frozen prompt contract. Whether fine-tuning removes modal-force
strengthening is a different paper; it is recorded as a limitation in
[`docs/experimental_setup.md`](docs/experimental_setup.md), not as planned work.
